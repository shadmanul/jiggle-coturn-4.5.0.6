version: "3"

services:
  server:
    build:
      context: ./
      dockerfile: Dockerfile
    image: shadmanul/coturn:4.5.0.6
    environment:
      EXTERNAL_IP: 139.59.248.179
    ports:
      - "3478:3478"
      - "3478:3478/udp"
    network_mode: "host"
    restart: "on-failure:10"

volumes:
  sqlite:
  logs: